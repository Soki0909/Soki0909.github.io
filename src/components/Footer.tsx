const Footer = () => {
  return (
    <footer className="bg-white mt-8 py-4 text-center text-sm text-gray-500">
      <p>
        &copy; {new Date().getFullYear()} KUME Soki (久米蒼輝). All Rights
        Reserved.
      </p>
    </footer>
  );
};

export default Footer;
