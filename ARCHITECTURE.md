# ポートフォリオサイト アーキテクチャ設計書

**📅 最終更新**: 2026年1月2日（リニューアル完了）  
**🎯 対象**: このサイトを開発・保守するAI・開発者

---

## 🎨 リニューアル概要（2026年1月）

### 目的

複雑すぎる9ページ構成を簡略化し、**Notionライクな2ページ構成**へ統合

### 達成事項

- ✅ Hub Page（タイムライン形式の経歴一覧）
- ✅ Document Page（個別プロジェクト詳細）
- ✅ 数学記号背景装飾（math-scatter.svg）
- ✅ 波形セクション区切り（WaveDivider）
- ✅ 詳細ページの画像・動画・リンク対応
- ✅ 旧ページのアーカイブ保存（`/_old`ルート）

---

## 🚨 必須事項 - 開発前に必読

### コミット前必須チェック（絶対厳守）

```bash
npm run format  # Prettier: コード整形
npm run lint    # ESLint: 166個のルール適用
npm run build   # TypeScript: 型チェック・ビルド確認
```

**❌ これらを実行せずにコミットすることは絶対に禁止**

### Git Commit規約（Conventional Commits）

```bash
feat: 新機能追加・機能拡張
fix: バグ修正・問題解決
refactor: リファクタリング・構造改善
docs: ドキュメント更新・README修正
style: スタイル・CSS・UI調整
chore: 設定・依存関係・ビルド修正
```

---

## 🏗️ アーキテクチャ構造

### Clean Architecture（5層構造）

```
src/
├── pages/               # プレゼンテーション層
│   ├── Hub.tsx          # メインページ（タイムライン）
│   ├── Document.tsx     # 詳細ページ
│   └── archive/         # 旧ページ（Home, About, Works等）
│
├── components/          # UIコンポーネント層
│   ├── TimelineView.tsx # タイムライン全体
│   ├── SignalNode.tsx   # 日付ノード
│   ├── WaveCard.tsx     # コンテンツカード
│   ├── WaveDivider.tsx  # 波形セクション区切り
│   └── SEO.tsx          # SEO・メタタグ
│
├── hooks/               # ビジネスロジック層
│   ├── useTimeline.ts   # タイムラインデータ・フィルタ
│   ├── useWritings.ts   # 執筆記事データ
│   └── useDetail.ts     # 詳細データ動的読み込み
│
├── data/                # データ層
│   ├── timeline.json    # タイムラインインデックス
│   ├── writings.json    # 執筆記事一覧
│   ├── profile.json     # プロフィール情報
│   └── details/         # 各項目の詳細データ
│       ├── piano-app.json
│       ├── sleep-buster.json
│       └── matlab-edm.json
│
├── types/               # 型定義層
│   └── dataModels.ts    # 全型定義集約
│
└── utils/               # ユーティリティ層
    ├── analytics.ts     # GA4統合
    └── structuredData.ts # 構造化データ
```

### 依存方向（厳守）

```
pages → components → hooks → utils → data/types
```

- ⬇️ **下向き依存のみ許可**
- ❌ **逆向き依存禁止**

---

## 📊 データ構造

### タイムラインデータ（timeline.json）

```typescript
interface TimelineItem {
  id: string; // "piano-app"
  date: string; // "2025-01"（ソート用）
  category: HubCategory; // "project" | "activity" | "writing"
  title: string;
  summary: string;
  tags: string[];
  externalLink?: string;
}
```

### 詳細データ（details/\*.json）

```typescript
interface DetailData {
  id: string;
  category: HubCategory;
  title: string;
  date: string;
  summary: string;
  tags: string[];
  content?: DetailContent; // overview, highlights, technologies, challenges, learned
  media?: DetailMedia; // images, videos, audios, youtube
  links?: DetailLinks; // github, demo, article, external
}
```

---

## 🎨 ビジュアル・アセット

### SVGアセット

| ファイル            | 用途                            |
| ------------------- | ------------------------------- |
| `math-scatter.svg`  | 数学記号背景（∫, ∑, π, ∞ など） |
| `damped-wave.svg`   | 減衰正弦波セクション区切り      |
| `timeline-axis.svg` | タイムライン軸線（未使用）      |

### アイコン

| ファイル   | 用途                         |
| ---------- | ---------------------------- |
| `logo.png` | 眼鏡アイコン（ヘッダー左上） |

---

## 🛠️ 技術スタック

| 技術         | バージョン | 用途             |
| ------------ | ---------- | ---------------- |
| React        | 19.1.1     | UIフレームワーク |
| TypeScript   | 5.8.3      | 型安全性         |
| Vite         | 7.1.3      | ビルドツール     |
| Tailwind CSS | 4.1.12     | スタイリング     |
| React Router | 7.8.2      | ルーティング     |

---

## 📊 パフォーマンス

| Core Web Vitals | 目標    | 達成値 |
| --------------- | ------- | ------ |
| LCP             | < 2.5s  | ~1.8s  |
| FID             | < 100ms | ~45ms  |
| CLS             | < 0.1   | ~0.05  |

---

## 🚀 デプロイフロー

```
git push → GitHub Actions → lint/build → GitHub Pages
```

**Live URL**: [https://soki0909.github.io](https://soki0909.github.io)

---

## 📁 旧ページアーカイブ

リニューアル前のページは `src/pages/archive/` に保存され、`/_old/*` ルートでアクセス可能：

- `/_old/home` - 旧ホームページ
- `/_old/about` - 旧アバウトページ
- `/_old/works` - 旧作品ページ
- `/_old/experience` - 旧経験ページ

---

## 🔧 緊急時対応

```bash
# 開発環境リセット
rm -rf node_modules package-lock.json
npm install
npm run dev

# 直前のコミットに戻す
git reset --hard HEAD~1
```

---

**💡 重要**: このアーキテクチャ設計書は、今後の開発・保守を担当するAI・開発者向けの技術仕様書です。新機能追加・改修時は必ずこの指針に従い、品質・保守性・パフォーマンスを維持しながら開発を進めてください。
